import React, { memo } from 'react';

interface IProps {}

const {{toCamelCase(name)}}Page: React.FC<IProps> = () => {
  return <div>{{toCamelCase(name)}}</div>;
};

export default memo({{toCamelCase(name)}}Page);
